<section class="section">
  <div class="container">
    <div class="columns">
      <%= render "topics" %>
      
      <div class="column is-half">
        <h1 class="title is-2 has-text-centered has-text-primary">Edit Your Post</h1>
        <%= render "posts/form", post: @post %>
      <%= render "feed" %>
      </div>

      <%= render "authors" %>
    </div>
  </div>
</section>